[pytest]
markers =
    integration: tests that hit the real Binance testnet API — requires .env.testnet
addopts = -m "not integration"
filterwarnings =
    ignore::DeprecationWarning:websockets
    ignore::DeprecationWarning:binance
